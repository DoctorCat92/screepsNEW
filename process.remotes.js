var processRemotes = {

    run: function(process) {
        
        // Поставить флаг RemoteMining на ресурс и указать комнату которая будет добывать 

        if (process.SourceList) {
            
        }
    } 
}   


module.exports = processRemotes;